enum Hives {
  customChains(
    id: 'customChains',
  );

  const Hives({required this.id});

  final String id;
}

enum HivesBox {
  rateLimit(
    id: 'rateLimit',
  );

  const HivesBox({required this.id});

  final String id;
}
